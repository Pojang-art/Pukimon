{
  "name": "pukimon",
  "version": "2.0.0",
  "description": "Chess Fantasy — static site (HTML/CSS/JS) untuk Cloudflare Pages.",
  "private": true,
  "scripts": {
    "start": "npx --yes serve -l 8000 .",
    "deploy": "npx --yes wrangler pages deploy . --project-name=pukimon"
  },
  "license": "ISC"
}
