{
  "name": "pukimon",
  "version": "2.1.0",
  "description": "Chess Fantasy — static site (HTML/CSS/JS) untuk Cloudflare Pages.",
  "private": true,
  "scripts": {
    "build": "node build.cjs",
    "start": "node build.cjs && npx --yes serve -l 8000 .",
    "deploy": "node build.cjs && npx --yes wrangler pages deploy . --project-name=pukimon"
  },
  "license": "ISC"
}
